import React from "react";
import ReactDOM from "react-dom/client";
import ChromaticApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ChromaticApp />
  </React.StrictMode>
);
